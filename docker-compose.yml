services:
  fincapp-backend:
    image: fincapp-backend:latest
    build:
      context: ./fincapp-backend
      dockerfile: Dockerfile
    ports:
      - "5080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__FincAppDb=Host=db.ekamlzyjrtqdsfihvang.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=tFFShjEeJPYTuEv5;SSL Mode=Require;
