services:
  fincapp-backend:
    image: fincapp-backend:latest
    build:
      context: ./fincapp-backend
      dockerfile: Dockerfile
    ports:
      - "5080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__FincAppDb=${FINCAPP_DB_CONNECTION}
